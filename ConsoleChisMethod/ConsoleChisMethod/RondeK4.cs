using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleChisMethod
{
    /*
   public class RondeK4
    {
        double[] x, y;
        double h;
        public RondeK4(double[] X, double[] Y, double n)
        {
            x = X;
            y = Y;
            h = X.Lenght / n;
        }
        public double Calcul(Fun func, double x1)
        {
            double[] res = new double[x.Lenght];
            //приблеженное решение в хi+1
            for (int i = 1; i < y.Lenght; i++)
            {

                var k1 = h * func(x[i - 1], y[i - 1]);
                var k2 = h * func(x[i - 1] + h/2, y[i - 1] + k1/2);
                var k3 = h * func(x[i - 1] + h / 2, y[i - 1] + k2 / 2);
                var k4 = h * func(x[i - 1] + h / 2, y[i - 1] + k3);
                res[i - 1] = y[i] + 1 / 6 * (k1 + 2 * k2 + 2 * k3 + k4);
            }
        }

    }
    */
}
